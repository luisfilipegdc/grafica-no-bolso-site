# Gráfica no Bolso — Landing Page

Landing page (site institucional / lista de espera) do **Gráfica no Bolso**.

Site estático: um único `index.html`, sem build nem dependências. Para
visualizar, abra o `index.html` no navegador.

Projeto separado do app — o sistema (SaaS) vive no repositório
`grafica-no-bolso`.

A copy reflete o posicionamento em `docs/estrategia.md` do projeto principal.
